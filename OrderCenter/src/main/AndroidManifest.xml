<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.me.guanpj.mall.order">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".module.order.confirm.OrderConfirmActivity" />
        <activity android:name=".module.address.list.AddressListActivity" />
        <activity android:name=".module.address.edit.AddressEditActivity" />
        <activity android:name=".module.order.list.OrderActivity" />
        <activity android:name=".module.order.detail.OrderDetailActivity" />
    </application>

</manifest>
